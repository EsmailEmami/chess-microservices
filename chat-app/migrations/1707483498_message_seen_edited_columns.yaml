---
up: |
  ALTER TABLE chat.message
    ADD is_edited BOOLEAN NOT NULL DEFAULT FALSE,
    ADD is_seen BOOLEAN NOT NULL DEFAULT FALSE;

down: |
  ALTER TABLE 
    DROP COLUMN is_seen,
    DROP COLUMN is_edited;
