---
up: |
  CREATE TABLE game (
    id    			    uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    white_player_id uuid NULL,
    black_player_id uuid NULL,
    turn VARCHAR(10) NOT NULL,
    moves jsonb null,
    pieces jsonb null,
    status INT NOT NULL,
 
    created_at 		  timestamptz default now(),
    created_by_id 	uuid null,
    updated_at 		  timestamptz default now(),
    updated_by_id 	uuid null,
    deleted_at		  timestamptz null,
    deleted_by_id 	uuid null,

    CONSTRAINT fk__game_user_created_by FOREIGN KEY (created_by_id) REFERENCES public.user (id) ON UPDATE CASCADE ON DELETE RESTRICT,
    CONSTRAINT fk__game_user_updated_by FOREIGN KEY (updated_by_id) REFERENCES public.user (id) ON UPDATE CASCADE ON DELETE RESTRICT,
    CONSTRAINT fk__game_user_deleted_by FOREIGN KEY (deleted_by_id) REFERENCES public.user (id) ON UPDATE CASCADE ON DELETE RESTRICT,

    CONSTRAINT fk__game_user_white_player FOREIGN KEY (white_player_id) REFERENCES public.user (id) ON UPDATE CASCADE ON DELETE RESTRICT,
    CONSTRAINT fk__game_user_black_player FOREIGN KEY (black_player_id) REFERENCES public.user (id) ON UPDATE CASCADE ON DELETE RESTRICT
  );

down: |
  drop table game;
